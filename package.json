{
  "name": "no-repeated-letters",
  "version": "1.1.1",
  "description": "Detect repeated letters in a word.",
  "keywords": [
    "word",
    "repetition",
    "jotto"
  ],
  "repository": "KenanY/no-repeated-letters",
  "license": "MIT",
  "author": "Kenan Yildirim <kenan@kenany.me> (http://kenany.me/)",
  "engines": {
    "node": ">=4"
  },
  "main": "dist/index.js",
  "jsnext:main": "dist/index.es.js",
  "files": [
    "index.js",
    "LICENSE.txt",
    "dist"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "rollup -c",
    "lint": "eslint *.js test/*.js",
    "tests-only": "npm run -s build && tape test/index.js",
    "coverage": "npm run -s build && nyc npm run -s tests-only",
    "pretest": "npm run -s lint && npm run -s build",
    "test": "npm run -s tests-only"
  },
  "dependencies": {
    "lodash.includes": "^4.3.0"
  },
  "devDependencies": {
    "@kenan/eslint-config": "^4.0.0",
    "eslint": "^3.4.0",
    "lodash.isfunction": "^3.0.8",
    "nyc": "^10.0.0",
    "rollup": "^0.41.1",
    "rollup-plugin-buble": "^0.15.0",
    "tape": "^4.6.0"
  }
}
